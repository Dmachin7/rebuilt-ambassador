services:
  - type: web
    name: rebuilt-ambassador
    runtime: node
    rootDir: server
    buildCommand: npm install && npx prisma db push && npx prisma generate
    startCommand: node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
